---
author: Panos G. Pgn
title: bsd
tags: bsd
date: 18/07/2010

&nbsp;
    man 7 security
&nbsp;
    /usr/ports/sysutils/bsdadminscripts/work/bsdadminscripts-3.0/src
    /sbin/dump -0u  -L -C16 -B4589840 -P 'growisofs -Z /dev/cd0=/dev/fd/0'
    maxproc in login.conf
&nbsp;
    sed -e '/^From / s, at ,@,' < source-file > other-file
    raw escape character: ctrl-v esc
    make fetch-recursive
    make package-recursive
    pw groupmod operator -m john
    make pretty-print-run-depends-list
    ctrl-v key
    shar `find . -print` > archive.shar
    mount_fusefs -v -o push_symlinks_in /dev/fuse0 /mnt/kafros sshfs -C -o idmap=user nanga@kafros.netstub.org:/
    mount_fusefs -v -o push_symlinks_in /dev/fuse0 /mnt/kafros sshfs nanga@kafros:/ -d
    mount_fusefs /dev/fuse0 /mnt/kafros
    umount /dev/fuse0#1
    fstat /dev/fuse0
    make CONFIGURE_ENV=LDFLAGS="-static" build
    fork bomb :() { :&:; } ;:
    "ctrl:swapcaps,grp:ctrls_toggle,grpled:scroll,altwin:left_meta_win,lv3:ralt"
* i get polish, german, russian and your modified french
* swapped caps lock and left ctrl
* two ctrls (i.e. old capslock+right ctrl) pressed at once switch the layout to the next one
* left windows key is meta (i am using this as a switch for dwm)
* right alt switches level 3 group (i.e. i get my polish letters with right alt+key)
&nbsp;
    ifconfig wlan0 192.168.1.9 netmask 255.255.255.0 broadcast 192.168.1.255
    route add default gw 192.168.1.1 wlan0
